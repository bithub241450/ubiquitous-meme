[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pricing-recorder"
version = "0.1.0"
description = "Tools for recording product pricing data from 21st Century Distributing."
authors = [{name = "AutoGenerated"}]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.12",
    "lxml>=5.3",
    "requests>=2.31",
]

[project.scripts]
pricing-recorder = "pricing_recorder.cli:main"
